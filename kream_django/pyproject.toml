[tool.poetry]
name = "kream-django"
version = "0.1.0"
description = "kream project on django"
authors = ["Ahla Ko"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
django = "^5.0.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
